(function () {
    'use strict';

    var app = angular.module('Tradency', [
        // Angular modules
        'ngRoute',
        'ngAnimate',
        'ngStorage',
        'ui.bootstrap'
    ]);

    app.run(['dataService', 'constant', function(dataService, constant) {

    }]);

})();

